# Silent mode
-s

# Makes curl show an error message if it fails.
-S

# Store the trace
--dump-header /tmp/curl_headers.txt

# Store the header info
#--trace curl_trace.txt

# Forces curl to use ipv4.
#ipv4
